class Solution(object):
    def subtractProductAndSum(self, n):
        dig=0
        pdt=1
        sum=0
        while n>0:
            dig=n%10
            sum+=dig
            pdt*=dig
            n = n // 10
        return (pdt - sum)
        """"
        :type n: int
        :rtype: int
        """
        
